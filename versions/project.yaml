# Written by ci-release in yadgarhq/project. Do not edit by hand.
# The digest governs the pull; the tag is here to be read.
# To roll back, set BOTH keys to the previous release.
# DO NOT DELETE THIS FILE. Deleting it hands helm no values,
# so the module falls back to the moving tag with nothing
# reporting it — ci / passed refuses the deletion (ledger
# 608, scripts/versions_pinned.py).
# That is an EDIT of both keys and never a delete, because
# :latest already points at the release being rolled back
# from.
# Retiring the module is not a reason to delete it either: a
# module leaves the cluster by losing its yadgar-deployable
# topic or its chart/, after which nothing reads this file
# and leaving it costs nothing.
image:
  digest: "sha256:2ee2ed07ab118406770b77fdcefae686b716b9d91ad786af65ae8fbba5e7b1e1"
  tag: "0.1.11"
